{
  "name": "cookingdb",
  "version": "1.0.0",
  "description": "CookingDB recipe application with static publishing, authoring, planning, and admin workflows",
  "type": "module",
  "dependencies": {
    "papaparse": "5.5.3"
  },
  "scripts": {
    "validate": "node scripts/validate.mjs",
    "build": "node scripts/build.mjs",
    "import:inbox": "node scripts/import-inbox.mjs",
    "report:missing-portions": "node scripts/report_missing_portions.js",
    "test": "node --check docs/site-behavior.js && node --check docs/built-data.js && node --check docs/recipe-model.js && node --check docs/recipe-repository.js && node --check docs/app.js && node --check docs/add.js && node --check docs/admin.js && node --check docs/bread-maker.js && node --check docs/planner.js && node --check docs/recipe.js && node -e \"import('papaparse').then(({default: Papa}) => { const out = Papa.parse('section,text\\nmain,\\\"line one\\nline two\\\"', {header:true}); if (out.errors.length || out.data[0].text !== 'line one\\nline two') process.exit(1); })\" && node scripts/check-architecture-contract.mjs && node scripts/check-site-behavior-contract.mjs && node scripts/check-data-loading-contract.mjs && node docs/recipe-model.test.mjs && node docs/recipe-repository.test.mjs && node docs/recipe-utils.test.mjs && node scripts/check-print-contract.mjs && node scripts/check-add-recipe-authoring-contract.mjs && node scripts/check-admin-inbox-review-contract.mjs && node scripts/check-publishing-safety.mjs",
    "test:browser": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "1.55.0"
  }
}
